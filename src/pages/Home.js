import { Grid } from '@mui/material';
import React from 'react';

export const HomePage = () => {
    return (
        <React.Fragment>
            <Grid>
                
            </Grid>
        </React.Fragment>
    );
}